info:
  name: track preview
  type: http
  seq: 6

http:
  method: GET
  url: "{{URL}}/api/tracks/{{trackId}}/preview"
  auth: inherit

runtime:
  variables:
    - name: trackId
      value: abc_my_dummy_track_id

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
