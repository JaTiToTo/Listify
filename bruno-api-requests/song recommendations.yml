info:
  name: song recommendations
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/api/songs/recommendations?limit=5"
  params:
    - name: limit
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
