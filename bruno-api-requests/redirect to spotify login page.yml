info:
  name: redirect to spotify login page
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/auth/login/spotify"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
